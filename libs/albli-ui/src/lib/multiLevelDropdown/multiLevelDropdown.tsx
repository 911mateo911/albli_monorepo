import { FC } from 'react';
import { MultiLevelDropdownProps } from './multiLevelDropdown.interface';

export const MultiLevelDropdown: FC<MultiLevelDropdownProps> = ({
  dropdowns,
  triggerOn,
  wrapperClassName
}) => {
  return (
    <div></div>
  )
}
